# Railway Deployment Environment Variables
# Copy these to Railway dashboard under your service's Variables tab

# Database Configuration (Railway will provide MySQL connection details)
DATABASE_URL=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=your_railway_mysql_password

# JWT Configuration (IMPORTANT: Generate a strong secret for production!)
JWT_SECRET=YourProductionSecretKeyMinimum256BitsLongForHS256Algorithm
JWT_EXPIRATION=21600000

# CORS Configuration (Set to your Vercel frontend URL)
CORS_ALLOWED_ORIGINS=https://your-app.vercel.app

# Spring Profile
SPRING_PROFILES_ACTIVE=prod

# Port (Railway assigns this automatically, usually not needed)
# PORT=8080
